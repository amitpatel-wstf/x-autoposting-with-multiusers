# X (Twitter) App creds — OAuth 2.0
X_CLIENT_ID=your_x_client_id
X_CLIENT_SECRET=your_x_client_secret
# Your app callback URL must match in Dev Portal (e.g., http://localhost:4000/auth/callback)
X_CALLBACK_URL=http://localhost:4000/auth/callback

# cookie/session
SESSION_SECRET=change_me

# app
PORT=4000